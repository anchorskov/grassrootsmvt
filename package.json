{
  "devDependencies": {
    "better-sqlite3": "^12.4.1",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "dependencies": {
    "jose": "^6.1.0"
  }
  ,
  "scripts": {
    "build:functions": "npx wrangler@4.42.0 pages functions build ./ui/functions --outdir ui/.wrangler && ls -R ui/.wrangler || true",
    "deploy:pages": "npx wrangler@4.42.0 pages deploy ./ui --project-name=grassrootsmvt --commit-dirty=true",
    "verify:functions": "curl -sS -H \"Authorization: Bearer $CF_API_TOKEN\" https://api.cloudflare.com/client/v4/accounts/$ACCOUNT_ID/pages/projects/$PROJECT/deployments?per_page=1 | jq -r '.result[0].uses_functions' || true",
    "verify:api": "echo 'replace <PREVIEW> with preview host' && curl -I https://<PREVIEW>.grassrootsmvt.pages.dev/api/healthz || true"
  }
}
