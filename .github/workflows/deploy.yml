---
name: "Deploy to Cloudflare Pages"

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: "🚀 Deploy to Cloudflare Pages"
    runs-on: ubuntu-latest

    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      PROJECT: grassrootsmvt

    steps:
      - name: "📥 Checkout repository"
        uses: actions/checkout@v4

      - name: "🧰 Setup Node.js"
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: "⚡ Cache npm dependencies"
        uses: actions/cache@v3
        with:
          path: |
            ~/.npm
            ui/functions/node_modules
          # Only include package-lock.json in the key when present. If not present
          # the cache key falls back to runner/os + npm so we still benefit.
          key: ${{ runner.os }}-npm-${{ hashFiles('ui/functions/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-

      - name: "🧰 Install Wrangler CLI (optional cached)"
        run: |
          echo "Installing wrangler (will use cache when available)..."
          npm install -g wrangler@4.42.1 || true

      - name: "Install functions dependencies"
        run: |
          echo "Checking for ui/functions/package.json"
          # Prefer clean deterministic installs with npm ci when a lockfile
          # exists. Fall back to npm install otherwise. If the lockfile is
          # missing in the repository, this keeps the build working while
          # encouraging reproducible CI when it's present.
          if [ -f ui/functions/package-lock.json ]; then
            echo "Found package-lock.json — running npm ci --prefix ui/functions"
            npm ci --prefix ui/functions
          else
            echo "No package-lock.json — running npm install --prefix ui/functions"
            npm install --prefix ui/functions
          fi

      - name: "🏗️ Build Cloudflare Pages Functions"
        working-directory: ui
        run: |
          echo "🏗️ Building Cloudflare Pages Functions..."
          mkdir -p .wrangler/functions-build
          if [[ -d "functions" ]]; then
            npx wrangler@4.42.1 pages functions build functions --outdir .wrangler/functions-build
          else
            echo "No functions directory found, skipping build."
          fi

      - name: "🚀 Deploy to Cloudflare Pages"
        working-directory: ui
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          echo "🚀 Deploying Cloudflare Pages from ui/..."
          set -o pipefail
          npx wrangler@4.42.1 pages deploy . --project-name=grassrootsmvt --commit-dirty=true 2>&1 | tee deploy.log
          EXIT_CODE=${PIPESTATUS[0]}
          if [ "$EXIT_CODE" -ne 0 ]; then
            echo "❌ wrangler deploy failed with exit code $EXIT_CODE"
            exit $EXIT_CODE
          fi
      - name: "📦 Upload deploy log"
        uses: actions/upload-artifact@v4
        with:
          name: deploy-log
          path: ui/deploy.log

      - name: "✅ Verify Cloudflare deployment"
        working-directory: ui
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          PROJECT: grassrootsmvt
        run: |
          node ../scripts/verify_deploy.mjs --project grassrootsmvt --retries 5 --interval 5000

      - name: "📦 Upload verification logs"
        uses: actions/upload-artifact@v4
        with:
          name: verification-results
          path: |
            verify__api_*.txt

      - name: "🔎 Lint this workflow with yamllint"
        run: |
          python -m pip install --upgrade pip
          pip install yamllint
          yamllint -c .yamllint .github/workflows/deploy.yml
